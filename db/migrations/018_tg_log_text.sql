-- Что именно ушло человеку.
--
-- В журнале до сих пор лежали только кампания, чат и время, а текст
-- собирался при отправке и терялся. На вопрос «что получила Варя» ответить
-- было нечем: пожелание выбирается случайно, а у Telegram нет способа
-- прочитать отправленное ботом. Для сообщений, которые мы посылаем сами,
-- без спроса, это неправильно.
--
-- Новых данных здесь не появляется: сообщение состоит из имён, которые в
-- базе и так есть.

alter table tg_log add column if not exists text text;
