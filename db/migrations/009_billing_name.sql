-- Имя для квитанции. В кабинете человек зовётся как ему привычно, а в
-- израильской отчётности имена должны выглядеть одинаково и латиницей,
-- иначе один и тот же родитель разойдётся по бухгалтерии на несколько.
-- Пусто — в квитанцию идёт обычное имя.
alter table users add column if not exists billing_name text;
