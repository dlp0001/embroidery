-- Лагерь идёт не каждый день подряд: обычно будни, а выходные пропускает.
-- Поэтому у группы с датами есть ещё и набор дней недели, по которым
-- занятия в этот период создаются.
alter table studio_groups
  add column if not exists weekdays int[] not null default '{}';
