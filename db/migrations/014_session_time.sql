-- Своё время у отдельного занятия.
--
-- Раньше время жило только у группы, и перенести один день было нельзя:
-- правка расписания двигала все занятия сразу. Теперь у дня может быть
-- своя отметка времени, и она главнее групповой. Пусто — значит, время
-- обычное, как у группы.

alter table studio_sessions add column if not exists starts_at time;
